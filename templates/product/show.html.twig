{% extends 'base.html.twig' %}

{% block body %}

    <h1> Product Show page</h1>

    <p>
        id={{ product.id }}
        <br>
        title= {{ product.title }}
        <br>
        price= &euro; {{ product.price }}
        <br>
        category= {{ product.category.name }}
        <br>
        description= {{ product.description }}
        <br>
        summary= &euro; {{ product.summary }}
        <br>
        category= {{ product.category.name }}

    </p>
{% endblock %}